<div class="form-new">
  <div class="input-form">
    <p style="color: green"><%= notice %></p>

    <%= render @post %>

    <div>
      <%= link_to t('common.edit'), edit_post_path(@post) %> |
      <%= link_to t('common.back'), posts_path %>

      <%= button_to t('common.destroy'), @post, method: :delete %>
    </div>
  </div>
</div>
